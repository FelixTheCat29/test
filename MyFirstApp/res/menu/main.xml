<!-- This declares that the Search action should appear as an action button
	 when room is available in the action bar, but the Settings action should
	 always appear in the overflow. (By default, all actions appear in the overflow, 
	 but it's good practice to explicitly declare your design intentions for each action.) -->

<menu xmlns:android="http://schemas.android.com/apk/res/android" >
    
    <!-- Search, should appear as action button -->
    <item android:id="@+id/action_search"
          android:icon="@drawable/ic_action_search"
          android:title="@string/action_search"
          android:showAsAction="ifRoom" />
    <!--  android:id specifies the action button as an element that can be referred to by the id -->
    <!--  android:icon specifies the image for this action button -->
    <!--  android:title is the string that the button is named -->
    <!--  android:showAsAction specifies if the button will be shown or will be in the overflow -->
    
    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_settings"
          android:title="@string/action_settings"
          android:showAsAction="never" />
</menu>